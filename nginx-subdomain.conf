# Nginx configuration for subdomain setup (e.g., trailerio.your-domain.com)
# Place this file at: /etc/nginx/sites-available/trailerio

server {
    listen 80;
    server_name trailerio.your-domain.com;  # Change to your desired subdomain

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Uncomment after running certbot for SSL
# server {
#     listen 443 ssl http2;
#     server_name trailerio.your-domain.com;
# 
#     ssl_certificate /etc/letsencrypt/live/trailerio.your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/trailerio.your-domain.com/privkey.pem;
# 
#     location / {
#         proxy_pass http://localhost:8081;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }

